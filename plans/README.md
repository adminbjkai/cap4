# Implementation Plans

Generated by the `/improve` skill on 2026-06-19 (advisor pass, commit `05737be`,
branch `feat/collapsed-doc-pipeline`). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Remediate dependency vulnerabilities (subscription-auth-safe bumps + overrides) | P1 | M | — | DONE (2026-06-19, applied to working tree, not yet committed/deployed) |
| 002  | Consolidate the two CI workflows and add a `pnpm audit` gate | P1 | S | 001 | DONE (2026-06-19, applied to working tree, not yet committed) |
| 003  | Fix `make smoke` + Playwright e2e + README to target the nginx host port | P2 | S | — | DONE (2026-06-19, applied to working tree, not yet committed/deployed) |
| 004  | Upgrade `apps/web` Vite 5 → 6/7 and restore the vitest 4.1 bump | P3 | M | 001 | TODO (scoped 2026-06-19; clears the last dev-tooling advisories) |

### Execution notes (2026-06-19)

- All three applied directly to the working tree and independently
  fresh-context-verified (PASS on 7 checks). **Not committed, not deployed** —
  left staged for owner review. Live containers run already-deployed artifacts,
  so the working-tree changes do not affect anything currently serving users.
- **001 deviations (lower-risk than planned):** transitive fixes done via root
  `pnpm.overrides` (`fast-uri`, `fast-xml-parser`, `ws`, `flatted`) instead of
  bumping `@aws-sdk`/`eslint` directly. `vitest` pinned to `~4.0.18` (NOT the
  planned `^4.1.0`) because vitest 4.1 drops vite-5 peer support and `apps/web`
  stays on vite 5 (vite-6 major deferred). Result: `pnpm audit --prod` clean
  (all request-path libs patched); residual dev-only advisories (vitest UI,
  vite-5 dev server, picomatch) documented in `DECISIONS.md #16`.
- **002:** CI audit gate uses `pnpm audit --prod --audit-level=high` (gates on
  shipped deps; won't red on the deferred dev-tooling advisories).
- **003:** `playwright.config.ts` deliberately left unchanged — its `webServer`
  block self-boots web-api on :3000, so that default is correct (Step-4 outcome).
  Only `make smoke`, `docker/nginx/default.conf` (+`/ready`), and `README.md`
  changed. **The nginx `/ready` change must be deployed (copy config + `nginx -s
  reload`) for `make smoke` to pass against the live stack — not yet done.**

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **002 depends on 001**: the CI audit gate (`pnpm audit --audit-level=high`)
  will fail the build until 001 has bumped the vulnerable packages. Land 001
  first, or the new gate immediately reds the pipeline.
- **003 is independent** of both and can be done in any order.

## Hard constraint that applies to ALL plans

All Claude model usage in cap4 runs through the **subscription/OAuth `claude`
CLI only — never the Anthropic API key or `@anthropic-ai/sdk`**. No plan may
introduce an `ANTHROPIC_API_KEY` env var, an SDK dependency, or any metered-API
model path. (Owner directive, 2026-06-19.) None of plans 001–003 touch model
auth; this is a guardrail for the executor, not a task.

## Findings considered and rejected (do not re-audit)

- **"Committed secrets in `.env`"** — FALSE. `.env` is not tracked and never
  appeared in git history; `.gitignore:20` excludes it. (A subagent fabricated
  this and quoted live secret values; discarded.)
- **"CI does not run tests"** — FALSE. `.github/workflows/test.yml` runs
  `pnpm test` on every push/PR to main, and `ci.yml` has a Postgres-backed
  `test` job. (The real gap — no unit tests for web-api/media-server — is a
  separate, un-selected finding.)
- **`stage-d.ts` `renderMarkdown` callback "signature mismatch"** — non-bug.
  TypeScript permits a 2-arg callback where a 3-arg type is expected; `step` is
  simply unused at `generate-doc.ts:197`.
- **Command injection in the doc pipeline** — none. `apps/worker/src/doc/exec.ts`
  uses `spawn(bin, args[], …)` with no shell; model args come from env/constants.
- **Micro defensive guards** (`thinFrames max>0`, etc.), **`HomePage` `React.memo`**,
  **library server-side status filter** — low value at single-user scale or guard
  impossible states; skipped per scope discipline.

## Findings selected but NOT yet planned (available on request)

These were surfaced and vetted in the audit but the owner chose only 1/4/5 for
this pass. Re-run `/improve` and ask for plans on any of:

- **SSRF allowlist misses private IP ranges + cloud-metadata IP**
  (`apps/web-api/src/routes/videos.ts:70-73`). Bounded impact (single-user).
- **No unit tests for web-api/media-server route logic** (idempotency, retry
  state machine, webhook HMAC verify) — characterization-test candidate; should
  precede any retry-endpoint refactor.
- **Retry endpoint over-broad + hand-rolled idempotency**
  (`apps/web-api/src/routes/videos.ts:557-684`). Known to owner.
- **Direction D2** — server-side HTML render of docs (deferred to P1 in
  `DECISIONS.md #13`).
